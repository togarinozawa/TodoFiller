<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="12dp"
        android:paddingTop="16dp"
        android:paddingBottom="8dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="単発タスク"
            android:textColor="@color/on_bg"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- 並び順と表示の切替。押すとその場でメニューが出る -->
        <Button
            android:id="@+id/sortButton"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="0dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:text="並び順"
            android:textColor="@color/primary"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- グループ／タグを横に並べるタブ。設定で出さないこともできる。
         塊を選んでいる間は全体が見えないので、階層をいじる操作は「すべて」タブだけ -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/taskTabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:tabMode="scrollable"
        app:tabGravity="start"
        app:tabIndicatorColor="@color/primary"
        app:tabSelectedTextColor="@color/primary"
        app:tabTextColor="@color/on_bg_dim" />

    <!-- 塊の中に小グループがある時だけ出る下段。上段より小さく、字も細くして
         「タブの中のタブ」だと見て分かるようにしてある -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/taskSubTabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/surface"
        android:visibility="gone"
        app:tabMode="scrollable"
        app:tabGravity="start"
        app:tabMinWidth="0dp"
        app:tabPaddingStart="12dp"
        app:tabPaddingEnd="12dp"
        app:tabIndicatorColor="@color/accent_goal"
        app:tabIndicatorHeight="2dp"
        app:tabSelectedTextColor="@color/accent_goal"
        app:tabTextColor="@color/on_bg_dim" />

    <!-- 片付いた数・増えた数。設定で消せる -->
    <TextView
        android:id="@+id/statsText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="6dp"
        android:paddingBottom="2dp"
        android:text=""
        android:textColor="@color/on_bg_dim"
        android:textSize="12sp"
        android:visibility="gone" />

    <!-- タスク追加（専用画面へ遷移） -->
    <Button
        android:id="@+id/addTaskButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:layout_marginBottom="8dp"
        android:backgroundTint="@color/primary"
        android:text="＋ タスクを追加"
        android:textColor="@color/white" />

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefresh"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recycler"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingBottom="16dp" />

            <TextView
                android:id="@+id/emptyText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="タスクはありません"
                android:textColor="@color/on_bg_dim"
                android:textSize="15sp"
                android:visibility="gone" />

            <!-- 掴んでいる間だけ出す「いま何段目か」の表示。
                 **リストの上に重ねる。** 帯として差し込むとリストの高さが変わり、
                 掴んだ瞬間に一覧全体が跳ねてカクつく。 -->
            <TextView
                android:id="@+id/dragHintText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|center_horizontal"
                android:layout_marginTop="8dp"
                android:background="@color/surface"
                android:elevation="8dp"
                android:paddingStart="14dp"
                android:paddingEnd="14dp"
                android:paddingTop="6dp"
                android:paddingBottom="6dp"
                android:text=""
                android:textColor="@color/primary"
                android:textSize="11sp"
                android:visibility="gone" />
        </FrameLayout>
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

</LinearLayout>
